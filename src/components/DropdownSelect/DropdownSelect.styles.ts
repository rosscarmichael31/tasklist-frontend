import styled from "styled-components";

const DropdownSelect = styled.select`
  padding: 10px;
  margin-bottom: 20px;
  font-size: 16px;
  border-radius: 5px;
  border: none;
  &:focus {
    outline: none;
  }
`;

const styles = { DropdownSelect };

export default styles;
