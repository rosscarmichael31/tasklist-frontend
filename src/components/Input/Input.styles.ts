import styled from "styled-components";

const Input = styled.input`
  padding: 10px;
  margin-bottom: 20px;
  font-size: 16px;
  border-radius: 5px;
  border: none;
  &:focus {
    outline: none;
  }
`;

const styles = { Input };

export default styles;
